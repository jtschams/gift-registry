// TODO: /join-family/:familyId
// Enter from LoginSignup component(with familyId param) or direct link (email)
// Calls "family" Query resolver
// Calls "joinFamily" Mutation resolver {MyProfile on submit}
//// Links to nothing
// Contains component Family via family
// Contains component RelatedUser via family.admins and family.members

export default function JoinFamily() {
  return
}